# TODO

- Benchmark accuracy against audiobook audio with known reference text.
- Add direct `whisper.cpp` support for macOS / Apple Silicon. It should perform better than the current `faster-whisper` CPU path through Podman.
- Build a better TUI.
